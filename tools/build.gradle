apply plugin: 'java'
apply plugin: 'maven'

version = '1.0-SNAPSHOT'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: new File((String)mvnrepo).absoluteFile.toURI().toURL().toString())
        }
    }
}
